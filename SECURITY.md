# Security

Do not commit API credentials.

Use GitHub Actions Secrets (e.g. `GITHUB_TOKEN`) and keep local secrets out of version control.

If you discover a security issue in the generator, contact the repository owner privately rather than publishing credentials or exploitable details.
